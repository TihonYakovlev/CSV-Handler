[project]
name = "youtube-clickbait-report"
version = "0.1.0"
description = "CLI application for building YouTube video metrics reports."
requires-python = ">=3.10"
dependencies = [
    "tabulate>=0.9.0",
]

[tool.pytest.ini_options]
addopts = ""
testpaths = ["tests"]
pythonpath = ["."]
